# リポジトリ監査 / PHASE 0

監査日: 2026-09-21。対象: `s8ev/adamapp`、既定ブランチ: `main`。

## 1. 開始時の全ファイル

GitHub APIのルート一覧、取得後の `git ls-tree -r HEAD`、履歴を照合した。
開始HEAD: `c431bdb`（Initial commit）。追跡ファイルは **README.md 1個のみ**。
内容は `# adamapp` と `adamapp`。Godot、シーン、コード、素材、テスト、CIは存在しない。
開始時のローカル作業フォルダは空。指定後にoriginを設定し、既存履歴を取得した。
親ディレクトリと作業フォルダに適用される既存AGENTS.mdはなかった。

## 2. 完成している機能

開始時: **なし**。READMEの存在をゲーム機能とは数えない。
今回追加した内容はCHANGELOGとQA_REPORTに記録し、開始時の状態と区別する。

## 3. 必要なシステム

| 系統 | 必要な機能 | 着手フェーズ |
| --- | --- | --- |
| 起動 | タイトル、シーン遷移、画面設定、入力、FPS、音量 | 0–1 |
| 操作 | 走行、ジャンプ、壁蹴り、ダッシュ、回避、スライド | 2 |
| 武器 | 射撃、装填、切替、拾得、投擲、奪取、近接 | 3 |
| AI | 視聴覚、警戒、捜索、共有、射撃、カバー、退避 | 4 |
| 戦闘演出 | 部位ダメージ、フィニッシャー、死亡、血、時間演出 | 5 |
| 会話 | 日本語、文字送り、感情、選択、ログ | 6 |
| 進行 | 型付きフラグ、条件、分岐、チャプター | 7 |
| 保存 | 原子的保存、復旧、世代移行、記録時間 | 8 |
| コンテンツ | M01、残り7章、通常/裏ボス、4結末、謎解き | 9–14 |
| 製品化 | 計測、最適化、互換性、QA、Windows配布 | 15–17 |

## 4. フォルダ構成

```text
project.godot             # Godot起点
scenes/
  boot/                  # 起動、タイトル
  ui/                    # 設定と開発用チェック画面
scripts/
  core/                  # 設定、シーン遷移
  player/                # 移動、状態描画、カメラ（PHASE 1追加）
  levels/                # 訓練場と地形データ（PHASE 1追加）
  effects/               # 時間効果・シェイク通知（PHASE 1追加）
  ui/                    # 画面ごとの振る舞い、共通UI
  visuals/               # コードで描く2Dピクセルアート
assets/
  fonts/                 # 日本語ドットフォントとライセンス
ui/                      # ピクセルUIテーマ
audio/                   # バス構成（音素材は後続フェーズ）
tests/                   # GDScriptによる独立した検証
tools/                   # 再現可能な導入、起動、検証
docs/                    # 仕様、判断、検証証跡
.github/workflows/       # 自動検証
```

基本InputMapはproject.godotに定義済み。入力再設定時に専用モジュールを追加する。
scenes/actors/player.tscnとscenes/levels/training.tscnはPHASE 1で実装した。
必要になったフェーズで `actors/enemies`、`combat`、`dialogue`、
`story`、`save`、`audio`、`effects` をscripts/scenesに追加する。
`resources/weapons`、`data/dialogue`、`data/chapters`、`assets/sprites`、
`assets/tilesets`、`assets/audio` も実データを作る際に追加する。
空クラスや動かない機能のスタブを大量に作らない。

## 5. 開発順序

ROADMAPのPHASE 0–17を順守。PHASE 10合格前のM02–08は文書設計だけ。
最初の縦切りはM01の5–10分。各機能の設計→実装→実行→修正→再試験を完結させる。

## 6. PHASE 0で実施する作業

- 横視点、ゲーム尺、操作、戦闘、公平性、世界観、分岐条件を決める。
- 必須11文書に、素材台帳、QA計画、現状監査、音仕様を追加する。
- 4.5.2を固定し、640×360の整数拡大と120Hz物理のプロジェクトを作る。
- 起動可能な小さな基盤を実装し、設計がGodot上で成立することを確認する。
- シーン・設定・入力を分離し、保存破損、画面遷移、フォント表示を検証する。
- 既存コミットを残し、機能ごとのコミットをGitHubへ反映する。
